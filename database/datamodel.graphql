# contains the definition of your data model which will be the foundation for the GraphQL CRUD API that’s generated by Prisma.

type Link {
  id: ID! @unique
  createdAt: DateTime!
  description: String!
  url: String!
}
